import styled from 'styled-components';

const CloseButton = styled.button`
  border: none;
  background-color: transparent;
  top: 15px;
  right: 16px;
  cursor: pointer;
`;

export { CloseButton };
